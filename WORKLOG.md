# 작업 기록

"NC 다이노스 경기 알림 앱을 만들고 싶다"는 한 문장에서 시작해, 실제로 폰에 알림이
오는 서비스가 되기까지의 과정을 커밋 순서대로 정리했다.

- 커밋 19개 · 자체 테스트 115항목 · 2026 시즌 135경기
- 배포 주소: https://nc-baseball-alert.nc-kbo-alert.workers.dev
- 브랜치: `feat/initial-app` (main 미병합)

---

## 요구사항 확정

경기 시작·취소·득점·종료 4종 알림, 일자별 기록 조회, 알림 종류별 on/off.
실행 방식은 PWA(홈 화면 추가로 앱처럼 동작), 서버는 Cloudflare Workers + D1로
정했다 — PC를 꺼도 알림이 오게 하는 것이 요구사항의 핵심이었다.

## 01 · PWA 초기 구현 — `eb25586`

- 네이버 스포츠 비공식 API로 KBO 일정·점수 조회
- Web Push를 **외부 패키지 없이** WebCrypto로 직접 구현 — VAPID(RFC 8292),
  aes128gcm 페이로드 암호화(RFC 8291/8188). `web-push` npm 패키지가 Node 전용
  crypto에 의존해 Workers에서 쓸 수 없었다
- 1분 주기 크론이 상태를 비교해 이벤트 감지, `dedup_key`로 중복 발송 방지

**검증**: 암호화한 페이로드를 수신자 키로 실제 복호화해 원문 일치를 확인했다 —
구현이 표준에 맞다는 것을 코드 리뷰가 아니라 왕복 테스트로 증명했다.

## 02 · 토스 스타일 UI, 시즌 게이팅, 보안 강화 — `467d841`

- 토스 디자인 언어(테두리 대신 배경 대비, 큰 라운드, 절제된 색)로 UI 구축
- 정규시즌·포스트시즌 구분 — gameId 접두사로 시리즈 판별
- 경기 시간대에만 폴링하도록 게이팅해 비시즌 API 호출을 하루 1회로 축소

**발견한 문제**: `/api/subscribe`가 임의 endpoint를 검증 없이 받아 서버가 그
주소로 직접 POST — SSRF에 열려 있었다. 허용 목록으로 막고, 문자열 조립 대신
DOM 생성 방식으로 바꿔 XSS 여지도 구조적으로 없앴다.

## 03 · Claude 디자인 시스템으로 전환 — `2e64996`

`awesome-design-md` 컬렉션에서 Claude 디자인을 골라 적용 — 크림 캔버스,
세리프 디스플레이, 코랄 강조. 이후 여러 커밋을 거치며 강조색만 NC 다이노스
실제 색(`ncdinos.com` 스타일시트에서 확인한 골드 `#c7a079`·남색 `#071d3d`)으로
계속 교체해 나갔다.

## 배포 · Cloudflare 최초 배포

D1 데이터베이스 생성, VAPID 키 발급, `workers.dev` 서브도메인 등록까지 단계별로
진행했다. 중간에 공개키·개인키가 서로 다른 실행에서 나와 `Invalid EC key` 오류가
났는데, `wrangler tail`로 실시간 로그를 잡아 원인을 특정하고 키를 재발급해 해결했다.

## 04 · NC 색상, Tossface 아이콘, 한국어 조사 처리 — `42c3ad2`

- Tossface는 이모지 전용 폰트(한글 글리프 없음) — SVG 심볼 5개만 인라인 추출해
  폰트 전체(12MB) 대신 7KB로 아이콘 확보
- es-hangul의 `josa()`로 "삼성과의", "두산을" 같은 조사를 정확히 처리 — 영문
  약어(KT→케이티)까지 한글 발음으로 읽어 판정한다

## 05 · 일정 탭, 홈경기 전용 알림 — `8f2380b`

일정 탭 신설, 홈경기를 좌측 골드 띠로 강조. `home_only` 설정을 추가해 발송
대상 조회 단계에서 원정 경기를 걸러내도록 구현했다.

## 06 · 이번 시즌 경기만 필터링 — `5a4c65d`

네이버 API에 시범경기·올스타전·지난 시즌 경기가 섞여 있는 것을 실제 응답을
뜯어 확인했다. 시범경기는 정규시즌과 형식이 완전히 같아 접두사로 구분이 안 돼,
순위 API의 `gameCount`로 개막일을 역산하는 방식을 썼다.

**실측**: 필터 전 147경기 → 필터 후 135경기. 제외된 12건이 정확히 3/12~3/24
시범경기였고, 개막일(3/28)이 10개 구단 데이터에서 전부 일치했다.

## 07 · VAPID 진단 메시지 개선 — `0a5e8ce`

실전에서 겪은 키 불일치 사고를 재발 방지 코드로 남겼다 — 키 누락, 형식 오류,
길이 불일치, 키쌍 불일치를 각각 구분되는 메시지로 잡아낸다.

## 08 · 순위 탭 — `be3ba76`

전체 10개 구단 순위표. 포스트시즌 진출 구간(한국시리즈/PO/준PO/와일드카드)을
하드코딩하지 않고 API 응답을 그대로 읽어 그린다 — KBO가 규칙을 바꿔도 따라간다.

## 09 · 일정 시즌 전체 · 기록 시작종료 시각 — `9dc9d68`

앞으로 30일 대신 시즌 전체(135경기)를 월별로. 시작·종료 시각은 알림 이벤트에
이미 기록된 실제 관측 시각을 재사용했다 — 새 데이터를 쌓지 않고 기존 것으로 해결.

## 10 · '오늘' 버튼, 달력 뷰 — `9fa84e0`

리스트/달력 전환과 오늘 위치 이동 버튼 추가. 7열 월간 격자, 홈/원정 구분, 날짜
클릭 시 리스트로 전환해 해당 경기로 스크롤.

## /simplify · 4관점 병렬 코드 정리 — `4dabbab`

재사용·단순화·효율·깊이 4개 에이전트를 병렬로 띄워 전체 코드를 리뷰했다. 중복
base64url 인코더 제거, VAPID 키 재사용으로 crypto 호출 N회→1회, `perspective()`
헬퍼를 세 곳에서 재구현하던 것을 통합 — 그중 하나는 서버 응답 형태까지
`/api/schedule`과 통일시켰다.

## 11 · 오늘 위치 자동 정렬, sticky 툴바 — `21859c6`

리스트가 켜질 때마다 오늘(없으면 다음) 경기로 중앙 정렬. 툴바를 화면에 고정하고
카드가 그 아래를 지날 때 점점 옅어지는 그라데이션 페이드 적용.

## 12 · 전광판, 상단 전체 고정, DB 마이그레이션 — `0d1c43d`

- 네이버의 새 엔드포인트(`/schedule/games/{id}/record`)를 조사로 발견 —
  이닝별 점수(R·H·E·B)를 제공한다
- 제목부터 요약줄까지 전부 고정, 카드만 스크롤되도록 구조 확장
- 배포된 D1에 `scoreboard` 컬럼을 `ALTER TABLE`로 직접 추가 — 스키마 변경을
  운영 중인 서버에 실제로 반영했다

**발견한 버그**: 툴바 높이 측정에 쓴 `ResizeObserver`의 첫 콜백이 비동기라,
스크롤이 먼저 일어나면 두 고정 영역이 겹쳤다. 등록 전 동기 측정을 추가해 최초
값을 보장하도록 고쳤다.

## 13 · 라이트/다크 전환, 탭 스와이프 — `eb3a415`

`data-theme` + localStorage로 시스템 설정과 별개로 테마를 고정할 수 있게 했다.
기록·일정·순위·알림을 좌우 스와이프로 전환 — 전광판처럼 자체 가로 스크롤이
있는 영역은 스와이프 판정에서 제외했다.

## 14 · 색상 조정, 로고를 오리지널 실루엣으로 — `8b7572c` · `b572d5d`

달력 승/패 색을 파랑/빨강으로 조정. 상단·하단의 별 모양 마크를 공룡 실루엣으로
교체했다 — **공식 구단 로고는 상표·저작권 대상이라 그대로 쓸 수 없어**, 처음엔
등판 3개짜리 스테고사우루스형으로, 이어서 긴 목·작은 머리의 브라키오사우루스형으로
오리지널 실루엣을 직접 그렸다.

**검증 방식**: SVG를 캔버스에 그려 픽셀을 읽고 흑백 아스키 아트로 뽑아 실제
형태를 눈으로 확인 — 스크린샷 도구가 이 세션에서 불안정해 택한 대안이었다.

---

전체 코드와 설치·배포 방법은 [README.md](README.md)에 있다.
