# 보안 점검 결과

이 문서는 전체 코드를 훑어 찾은 문제와 조치를 기록한다.
자동화 스캐너 없이 손으로 읽어 정리했으므로, "발견하지 못한 것"이 "없는 것"은 아니다.

점검 시점: 2026-08-23 · 대상: `src/`, `public/`, `wrangler.toml`, `schema.sql`

---

## 조치한 문제

### 1. SSRF — 임의 주소로 서버가 요청을 보낼 수 있었음 · 심각

가장 위험했던 지점이다. `/api/subscribe` 는 클라이언트가 준 `endpoint` 를 그대로 저장하고,
알림 발송 시 그 주소로 **서버가 직접 POST** 한다. 검증이 없으면 누구나 이렇게 할 수 있었다.

```
POST /api/subscribe { "endpoint": "http://192.168.0.1/admin", ... }
```

Worker 를 임의의 주소로 요청을 날리는 도구로 쓸 수 있고, 클라우드 내부망이나
서드파티 서비스를 향하게 만들 수도 있다.

**조치** — 알려진 푸시 서비스 호스트만 허용한다 (`src/security.js`).
`https` 강제, 호스트 정확 일치 또는 도메인 경계를 지키는 접미사 일치, 길이 상한.
새 브라우저가 새 호스트를 쓰기 시작하면 `EXTRA_PUSH_HOSTS` 환경변수로 코드 수정 없이 풀 수 있고,
거부된 호스트는 로그에 남는다.

### 2. XSS — 외부 데이터를 innerHTML 로 조립 · 중간

기록 화면이 팀명·구장·`statusInfo`("7회말") 같은 값을 문자열 템플릿으로 이어 붙여
`innerHTML` 에 넣고 있었다. 이 값들은 전부 네이버 API에서 온다. 지금 그 API가 안전하다는 것은
현재 상태일 뿐이고, 그 가정 위에 화면을 세울 이유가 없다.

**조치** — 문자열 조립을 걷어내고 DOM 을 직접 만든다 (`public/app.js` 의 `el()`).
모든 텍스트가 `textContent` 로만 들어가므로 태그가 해석될 경로 자체가 없다.
더해서 CSP `script-src 'self'` 로 인라인 스크립트를 막아 한 겹을 더 뒀다.

### 3. 인증 없는 관리 기능 · 중간

`/api/poll` 이 누구나 호출 가능했다. 호출 한 번마다 외부 API 조회와 D1 쓰기가 일어나므로
반복 호출로 요금과 무료 티어 한도를 소모시킬 수 있었다.

**조치** — `/api/admin/poll` 로 옮기고 `ADMIN_TOKEN` 시크릿을 요구한다.
**토큰을 설정하지 않으면 항상 401** 이다(기본 거부). 토큰 비교는 상수 시간으로 한다.

### 4. 테스트 알림 남용 · 중간

`/api/test` 에 제한이 없어, 엔드포인트를 아는 쪽이 특정 기기에 알림을 무제한으로 보낼 수 있었다.

**조치** — 구독당 30초 쿨다운(`last_test_at`).

### 5. 무제한 구독 등록 · 낮음

구독 개수 상한이 없어 DB 를 채울 수 있었다.

**조치** — 200개 상한. 기존 구독 갱신은 상한과 무관하다.

### 6. 입력 검증 부재 · 낮음

`p256dh` / `auth` 를 형식 확인 없이 저장했다. 잘못된 값은 저장 시점이 아니라
**발송 시점에** 예외로 터져 원인 추적이 어려워진다.

**조치** — base64url 문자 집합과 디코딩 후 바이트 수(65 / 16)를 입력에서 검증한다.

### 7. 요청 본문 크기 무제한 · 낮음

**조치** — `Content-Length` 로 먼저 거르고 본문도 4KB 로 제한한다.

### 8. 내부 오류 메시지 노출 · 낮음

`catch` 에서 `err.message` 를 그대로 응답에 실었다. 스택이나 내부 구조가 새어 나갈 수 있다.

**조치** — 클라이언트에는 일반 메시지, 상세는 서버 로그로만.

### 9. CSRF 방어 명시 부재 · 낮음

`Content-Type: application/json` 이 프리플라이트를 유발해 실질적으로는 이미 막혀 있었지만,
의도가 코드에 없었다.

**조치** — 상태를 바꾸는 요청에 동일 출처 검사를 넣었다.
`Origin` 이 없는 요청(curl 등)은 브라우저가 아니므로 통과시킨다 — CSRF 는 피해자의 브라우저를
이용하는 공격이라 `Origin` 이 반드시 붙는다.

### 10. 보안 헤더 없음 · 낮음

**조치** — 정적 응답에 `Content-Security-Policy`, `X-Content-Type-Options: nosniff`,
`Referrer-Policy: same-origin`, `Permissions-Policy`, `frame-ancestors 'none'`,
`base-uri 'none'`, `form-action 'none'` 을 붙인다.

---

## 점검했고 문제없던 것

**SQL 인젝션** — 모든 값은 `bind()` 로 넘어간다. SQL 문자열에 들어가는 유일한 동적 요소는
컬럼명인데, `KIND_COLUMN` / `SCOPE_COLUMN` 이라는 코드 내 화이트리스트에서만 나온다.
클라이언트 입력이 식별자 위치로 흘러갈 경로가 없다.

**시크릿 관리** — `VAPID_PRIVATE_KEY` 와 `ADMIN_TOKEN` 은 Cloudflare 시크릿으로만 다룬다.
`wrangler.toml` 에는 자리표시자만 있고, `.gitignore` 가 `.dev.vars` 와 `.env` 를 막는다.
저장소 전체에서 실제 키 값은 발견되지 않았다.

**푸시 암호화** — RFC 8291/8188 구현을 수신자 키로 실제 복호화해 원문 일치를 확인했다
(`npm test`). 같은 평문이 매번 다른 암호문이 되는 것(salt·임시키 재사용 없음)도 확인했다.

**중복 발송** — `dedup_key` UNIQUE + `INSERT OR IGNORE` 로 크론 중복 실행 시에도
같은 알림이 두 번 나가지 않는다.

---

## 남아 있는 위험 (알고 두는 것)

**엔드포인트가 곧 권한이다.** 구독 엔드포인트를 가진 쪽은 그 구독의 알림 설정을 바꾸거나
테스트 알림을 보낼 수 있다. 엔드포인트는 추측 불가능한 고엔트로피 값이고 기기와 서버 밖으로
나가지 않으므로, 이를 얻으려면 이미 DB나 기기에 접근한 상태여야 한다.
개인용 서비스 규모에서 별도 토큰 체계를 더할 실익이 없다고 판단했다.
공용으로 열 계획이라면 구독 시 별도 토큰을 발급하는 방식으로 바꿔야 한다.

**공개 GET 엔드포인트에 요율 제한이 없다.** `/api/history`, `/api/standings` 는 인증이 없다.
읽기 전용이고 캐시가 붙어 있지만, 남용 징후가 보이면 Cloudflare 대시보드에서
Rate Limiting 규칙을 거는 것이 가장 간단하다.

**네이버 API 의존.** 비공식·비버전 API다. 응답 구조가 바뀌면 알림이 조용히 멈출 수 있어
응답 형태를 검증해 예외를 던지고 로그를 남기게 했다. 다만 **자동 복구는 없다.**
`wrangler tail` 로 `response shape changed` 가 보이면 `src/kbo.js` 를 고쳐야 한다.

**"경기 중" 상태값 미검증.** 개발 시점에 진행 중인 경기가 없어 실제 값을 확인하지 못했다.
`BEFORE`/`RESULT` 가 아니면 진행 중으로 간주하는 방어적 처리를 했고 원본 값을 DB에 남긴다.
